@Library("dst-shared@release/shasta-1.4") _

dockerBuildPipeline {
    repository = "cray"
    imagePrefix = "cray"
    app = "spire-tokens"
    name = "spire-tokens"
    description = "Service for issuing spire join tokens"
        product = "csm"
}